To compile on Z80 using Aztec C:

    cz 4
    as 4
    ln 4.o -lutil -lc

(assumes paths are set up properly and CPM can locate util.lib and
c.lib)

On my RC2014, it runs on my input in 100 seconds.
